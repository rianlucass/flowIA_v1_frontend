import { DashboardView } from '@/views/dashboard/DashboardView';

export const metadata = {
  title: 'Dashboard — TesteIA',
};

export default function DashboardPage() {
  return <DashboardView />;
}
